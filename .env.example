# copy this file to .env and fill in your real keys — .env is gitignored, never commit it

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# only needed once the fine-tuned judge is deployed as an HF Inference Endpoint
HUGGINGFACE_API_TOKEN=hf_...
JUDGE_MODEL_ENDPOINT=https://api-inference.huggingface.co/models/jahnavi-reddy03/llama3-8b-code-judge
